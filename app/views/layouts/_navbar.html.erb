<nav class="navbar navbar-toggleable-md navbar-light bg-faded">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand" href="/">Store</a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/products">Products <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/cart">
          Cart
          <% if false %>
            <span class="badge badge-pill badge-info hide">10</span>
          <% end %>
        </a>
      </li>
    </ul>

    <ul class="navbar-nav mr-auto float-right login-buttons">
      <% unless current_user.present? %>
        <li class="nav-item"><%= link_to 'Sign in', new_user_session_path, class: 'btn btn-primary' %></li>
        <li class="nav-item"><%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-primary' %></li>
      <% else %>
        <li class="nav-item"><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
      <% end %>
    </ul>
  </div>
</nav>
